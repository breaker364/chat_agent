## 1. 错误分类基础(P0)

- [x] 1.1 新增单测:超时/网络类异常映射为可重试类别;参数/权限/策略/预算/业务拒绝映射为不可重试类别;未知异常默认可重试。
- [x] 1.2 实现通用错误分类模块(异常类型与状态语义到 `error_category`/`retryable` 的映射),并在工具包装层把失败结果与异常规范化为携带分类字段的结构。

## 2. 修复循环消费分类(P0)

- [x] 2.1 新增单测:不可重试错误的同一调用指纹不再注入下一轮修复;可重试错误维持有界修复路径;修复轮次上限行为不变。
- [x] 2.2 实现 `agent.py` 修复循环读取失败记录的 `retryable` 并短路不可重试的重复调用。

## 3. 失败指纹反馈泛化(P1)

- [x] 3.1 新增单测:任意工具同一动作指纹第二次失败结果携带一次性反重试提示;首次失败无提示;第三次起不堆叠;成功后计数清空。
- [x] 3.2 在工具包装层实现运行内失败指纹计数与提示追加,`fix-agent-tool-reliability` 草案的 bash 命令指纹实现迁移至该通用路径。

## 4. action_id 透出(P1)

- [x] 4.1 新增单测:副作用工具的结果与 `tool_runtime_*` 审计事件携带 `action_id`;同一动作重复派发时 `action_id` 稳定;值与 `build_idempotency_key`/`_dedupe_key` 推导一致。
- [x] 4.2 实现结果与审计链路的 `action_id` 字段填充(拦截、复用、执行三类动作均携带)。

## 5. 变更后校验 post-check(P1)

- [x] 5.1 新增单测:回读一致时 `verified=True` 入账;回读不一致时结果标记 `verification_failed` 且不入成功台账;无回读通道时入账且 `verified=False` 并明示未验证。
- [x] 5.2 实现校验回调与 `MutationLedger` 集成,`record_success` 的 `verified` 改为仅由真实校验产生。

## 6. 变更前检查 pre-check(P2)

- [x] 6.1 新增单测:目标状态精确匹配期望时跳过派发并标记 `already_satisfied` 入台账;不精确匹配时正常派发;目标不可读时跳过检查直接派发。
- [x] 6.2 实现 mutating skill 派发前状态读取与精确匹配跳过。

## 7. 复测与验收

- [x] 7.1 运行后端全部测试,确认零回归。
- [ ] 7.2 重跑基准中含失败重试场景的任务,审计工具调用:同一失败命令的原样重复次数降为零,不可重试错误不再占用修复轮次,blocked 率不升高。
