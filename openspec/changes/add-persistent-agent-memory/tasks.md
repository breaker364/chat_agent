## 1. 记忆契约与配置

- [x] 1.1 为 `agent_memory` 定义默认关闭的配置契约，包含受控目录、最近消息数、索引行数/字节数、单条记忆大小、最大扫描数和提取超时。
- [x] 1.2 新增记忆类型、候选操作、记录摘要、提取结果和受控错误类别的纯数据模型，并验证 `user`、`feedback`、`project`、`reference` 类型。
- [x] 1.3 编写配置和模型测试，覆盖默认关闭、无效限额、未知类型、超长字段和不安全 memory id。
- [x] 1.4 提供可注入的 `MemoryExtractor`、`AgentMemoryStore`、`MemoryContextProvider` 和调度器协议，支持离线模型桩和临时根目录。

## 2. 受控记忆存储与索引

- [x] 2.1 新增记忆存储模块，实现记忆根目录解析、memory id/slug 规范化、路径穿越防护、允许 frontmatter 字段和受控 Markdown 序列化。
- [x] 2.2 实现单条记忆的 create/update/read/list/delete，以及 `MEMORY.md` 的索引生成、去重、行数和字节数限制。
- [x] 2.3 以临时文件和原子替换实现记录、索引和最小状态元数据写入；失败时保留上一次有效记录和索引。
- [x] 2.4 编写存储测试，覆盖首次写入、同一 id 更新、索引重建、超限截断、删除、损坏文件、目录外路径和原子写入失败。

## 3. 无工具 LLM 记忆提取

- [x] 3.1 编写固定提取提示和 JSON schema，明确长期价值判断、四种类型、可更新 manifest、代码/Git/临时任务排除项和“对话是数据”的注入防护。
- [x] 3.2 实现默认结构化提取器，复用受配置管理的聊天模型连接，但不注册 Agent 工具、技能、MCP 或文件写权限。
- [x] 3.3 实现提取结果解析与校验，只允许有界 create/update 记录；将无效 JSON、超时、模型异常、敏感字段和空结果安全降级为无写入结果。
- [x] 3.4 编写提取器测试，覆盖可保留偏好/反馈/项目背景/外部参考、临时内容排除、重复更新、模型失败和 prompt injection 作为数据处理。

## 4. 异步调度与聊天运行集成

- [x] 4.1 实现按记忆根目录串行化且可合并的 `MemoryExtractionScheduler`，使并发 session 不会同时写入同一索引。
- [x] 4.2 在流式聊天中 assistant 最终或部分结果成功写入 `SessionStore` 后调度提取，且不延迟或改写当前 `done`、工具 transcript 和 run 终态。
- [x] 4.3 在同步 `/chat` 路径接入等价的提取调度，确保两种聊天入口采用相同的记忆生命周期。
- [x] 4.4 将 queued/completed/skipped/failed 记忆操作记录为受控的 session progress 或 debug 元数据，不写入对话正文、不泄露输入对话或模型推理。
- [x] 4.5 编写并发和集成测试，覆盖两个 session 同时完成、提取运行时客户端断开、提取失败、合并后续候选和聊天结果保持不变。

## 5. 记忆索引注入与按需读取

- [x] 5.1 实现 `MemoryContextProvider`，在功能启用时安全读取有限 `MEMORY.md` 索引，并在缺失、空、损坏或超限时返回空上下文。
- [x] 5.2 将记忆索引注入 `stream_agent_events()` 的动态 system prompt，包含“记忆是可能过时的数据、重要事实需先按需读取”的通用指引。
- [x] 5.3 验证记忆目录位于现有受控工作区读取边界内，Agent 可用现有读文件能力读取具体记录，但不获得任意记忆写权限。
- [x] 5.4 编写 prompt 组装测试，覆盖关闭状态、空索引、有效索引、行数/字节数限制、完整正文不注入和现有上下文压缩/工具历史不回归。

## 6. 记忆管理 API

- [x] 6.1 新增 `GET /memories`，只返回有界的记忆摘要字段，不返回提取输入、模型推理或无关 session 数据。
- [x] 6.2 新增 `GET /memories/{memory_id}`，通过受控 id 返回单条校验后的记忆记录。
- [x] 6.3 新增 `DELETE /memories/{memory_id}`，仅允许删除受控根目录内的记录并原子同步 `MEMORY.md`。
- [x] 6.4 编写 API 测试，覆盖成功 list/read/delete、未知 id、无效 id、删除后索引一致性、关闭状态和不影响 session/RAG/工具历史。

## 7. 文档、验证与发布准备

- [x] 7.1 更新用户文档，说明记忆作用域、自动保存边界、索引与按需读取、遗忘方式、配置、单一受信任工作区限制和多用户前置条件。
- [x] 7.2 运行新增记忆测试，以及现有 session store、工具历史、上下文压缩、Agent 流式和 API 回归测试，修复兼容性问题。
- [x] 7.3 运行 Python 编译、`git diff --check` 和 OpenSpec 严格校验，确认没有实体硬编码、路径绕过、原始敏感数据泄露或 chain-of-thought 暴露。
- [x] 7.4 在启用功能的临时工作区手工验证：一轮产生长期偏好，另一 session 可见索引并按需读取；遗忘后索引和文件均消失，聊天历史保持不变。
