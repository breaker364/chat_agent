## 1. 异常分析后端

- [ ] 1.1 为异常分析整理并脱敏本次请求的异常摘要、活动状态、工具调用/错误摘要和部分结果上下文。
- [ ] 1.2 实现结构化中文异常分析调用、输出校验与安全格式化，确保有效模型分析优先于规则化文案。
- [ ] 1.3 保留并收紧规则化异常兜底，使模型超时、调用失败、格式错误或不安全输出时仍能稳定完成终止响应。
- [ ] 1.4 为有效分析、模型不可用、无效结构和敏感内容脱敏补充后端单元测试。

## 2. 工作动态事件

- [ ] 2.1 定义并实现 `activity` SSE 负载生成器，包含状态、摘要、证据、判断、下一步、可选进度和已用时间。
- [ ] 2.2 在请求开始、工具生命周期、修复重试、正常完成和终止失败时发送与实际执行一致的工作动态。
- [ ] 2.3 对连续心跳和同一工作阶段进行合并，避免重复时间线事件，并确保用户消息不含隐藏推理或原始诊断数据。
- [ ] 2.4 为活动事件顺序、合并行为、完成和受阻终态补充流式后端测试。

## 3. 前端对话内时间线

- [ ] 3.1 扩展 SSE 解析与本次请求状态，接收、合并并保存 `activity` 事件。
- [ ] 3.2 实现默认展开的 `Agent 工作动态` 组件，在聊天流中实时显示摘要、证据、判断、下一步和进度。
- [ ] 3.3 在 `done` 和 `error` 时将时间线分别收束为完成和受阻状态，保留最终回答与历史工作动态。
- [ ] 3.4 调整样式以适配桌面和窄屏布局，并确认 Debug stream、工具事件分组和任务进度侧栏仍可独立使用。

## 4. 集成验证

- [ ] 4.1 运行后端测试，覆盖正常任务、工具失败和异常分析模型降级路径。
- [ ] 4.2 运行前端测试或构建，并验证 `activity` 事件可在真实 SSE 流中实时呈现。
- [ ] 4.3 进行浏览器端人工验收，检查时间线默认展开、状态收束、中文可读性和 Debug stream 保留。
