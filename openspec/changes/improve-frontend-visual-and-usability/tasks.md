## 1. 主题体系与视觉系统

- [x] 1.1 审查 `App.css` 剩余硬编码颜色并补全设计令牌,新增 `[data-theme="dark"]` 暗色令牌集,校验暗色下正文/次要文本对背景达到 WCAG AA 对比度。
- [x] 1.2 实现主题切换控件(浅色/暗色/跟随系统)与 localStorage 持久化,"跟随系统"监听 `prefers-color-scheme` 变化。
- [x] 1.3 在 `frontend/index.html` 增加渲染前主题初始化脚本防闪烁,并添加 favicon 与运行状态页面标题(任务运行中/空闲)。
- [x] 1.4 引入 `rehype-highlight`,为 Markdown 代码块提供语法高亮与复制按钮,高亮主题适配浅色/暗色两套令牌。
- [x] 1.5 实现全局轻量 toast 组件(成功/失败两态,自动消失),接入知识库导入/同步/删除与技能安装/执行的结果反馈。
- [x] 1.6 泛化头部能力徽标:移除硬编码实体徽标,技能徽标改为基于已安装技能列表动态生成;`iconForTool` 改为通用类别判断并移除领域关键词分支。
- [x] 1.7 将主要界面控件文案统一为中文(按钮、占位符、空态、键盘提示),同步更新受影响的前端测试断言。
- [x] 1.8 为主题选择、持久化、系统偏好跟随与防闪烁初始化补充组件测试。

## 2. 圆角、动效与毛玻璃质感

- [x] 2.1 上移并统一圆角令牌,应用到消息气泡、能力卡片、对话框与按钮;输入区改为胶囊形态;头像与操作按钮全圆;调试流/工具参数等数据密集面板保留小圆角。
- [x] 2.2 建立标准动效集:新消息与工作动态项入场(淡入上移)、对话框/技能弹窗/知识中心与遮罩过渡、toast 滑入滑出、按钮与卡片悬停微交互、流式等待脉冲指示;动画仅用 transform/opacity,避免布局抖动与滚动跳变。
- [x] 2.3 全部装饰性动效接入 `prefers-reduced-motion` 降级,减弱动态开启时替换为瞬时切换,状态信息保持完整。
- [x] 2.4 实现毛玻璃材质:顶栏改为吸顶玻璃、输入区外壳与模态遮罩应用 backdrop-filter,新增两套玻璃令牌,`@supports` 不支持时降级为半透明实色,持久模糊表面不超过 3 处。
- [x] 2.5 双主题下逐屏走查圆角一致性、动画流畅度与滚动性能(长会话场景),以令牌/类名断言补充对应前端测试。

## 3. 消息级交互

- [x] 3.1 抽取 `MessageActions` 组件:助手消息 hover 显示复制(Clipboard API 带降级)与点赞点踩(本地记录,后端反馈端点就绪后上报)。
- [x] 3.2 实现重新生成:仅最后一条助手消息可用,基于其对应最后一条用户消息经现有 `/chat/stream` 重发,不修改历史。
- [x] 3.3 实现用户消息编辑回填到输入框(非破坏式),为消息添加相对时间显示(悬停展示完整时间戳)。
- [x] 3.4 工具调用气泡接入真实状态:按调用/结果配对推导运行中、完成、失败三态,替换写死的 "running" 文本,保持默认折叠。
- [x] 3.5 终止性错误消息增加"重试"按钮(复用重新生成路径),聊天区新增滚动离底时的"回到底部"浮标。
- [x] 3.6 为消息操作栏、工具状态配对、重新生成与重试补充前端测试。

## 4. 会话管理易用性

- [x] 4.1 抽取通用 `Dialog`/`ConfirmDialog`/`PromptDialog` 原语:遮罩、Esc 关闭、focus trap、`aria-modal` 与初始焦点管理。
- [x] 4.2 会话重命名与删除改用应用内对话框,替换 `window.prompt`/`window.confirm`;删除确认需展示会话标题。
- [x] 4.3 会话列表增加搜索过滤(标题即时过滤)与时间分组(今天/昨天/本周/更早),会话项显示相对更新时间。
- [x] 4.4 为会话对话框交互、搜索过滤与时间分组补充前端测试。

## 5. 空状态引导

- [x] 5.1 实现 `Welcome` 空状态组件:通用能力卡片(联网检索、文件分析、知识库问答、技能执行)与建议提示词列表,点击回填输入框不自动发送。
- [x] 5.2 建议词支持配置文件覆盖默认列表,并基于已安装技能的名称与描述动态追加示例;确保默认文案不含具体实体名词。
- [x] 5.3 为空状态渲染、配置覆盖与回填交互补充前端测试。

## 6. 可选后端支持与集成验证

- [ ] 6.1 (可选)后端新增最小 `POST /feedback` 端点接收会话/消息级反馈并落盘,前端反馈上报接入该端点,失败降级本地。
- [ ] 6.2 运行前端全部测试与生产构建,浅色/暗色两主题下逐屏人工走查(会话侧栏、聊天流、知识库、技能弹窗、对话框)。
- [ ] 6.3 浏览器端人工验收:主题切换与刷新无闪烁、消息操作可用、会话管理无原生弹窗、空状态引导出现且无实体硬编码文案。
